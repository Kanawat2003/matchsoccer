import {useEffect,useState} from 'react'
import AdminDashboard from './AdminDashboard'
import OwnerDashboard from './OwnerDashboard'
import OwnerBookings from './OwnerBookings'
import './App.css'
import Manage from './Manage'
import {api} from './api'
import type {Venue,Slot,Match,MatchPlayer,User,BookingRow,SplitBill,Notification} from './api'

type HomeProps={query:string;setQuery:(s:string)=>void;venues:Venue[];matches:Match[];select:(v:Venue)=>void;nav:(s:string)=>void;venueError:boolean;retry:()=>void}
export default function App(){
 const splitPath=window.location.pathname.match(/^\/split\/([a-f0-9]{48})$/i)
 const [tab,setTab]=useState('home'),[notifications,setNotifications]=useState<Notification[]>([]),[showNotifications,setShowNotifications]=useState(false),[venues,setVenues]=useState<Venue[]>([]),[matches,setMatches]=useState<Match[]>([]),[query,setQuery]=useState(''),[selected,setSelected]=useState<Venue|null>(null),[user,setUser]=useState<User|null>(null),[auth,setAuth]=useState(false),[venueError,setVenueError]=useState(false)
 const refreshMatches=()=>api.matches().then(setMatches).catch(console.error)
 const loadVenues=()=>api.venues(query).then(v=>{setVenues(v);setVenueError(false)}).catch(()=>{setVenues([]);setVenueError(true)})
 useEffect(()=>{void api.venues(query).then(v=>{setVenues(v);setVenueError(false)}).catch(()=>{setVenues([]);setVenueError(true)})},[query])
 useEffect(()=>{void api.matches().then(setMatches).catch(console.error)},[tab])
 useEffect(()=>{const updated=(e:Event)=>{const u=(e as CustomEvent<User>).detail;if(u)setUser(u)};window.addEventListener('porsball:user-updated',updated);return()=>window.removeEventListener('porsball:user-updated',updated)},[])
 useEffect(()=>{let cancelled=false;const expired=()=>{localStorage.removeItem('porsball_token');if(!cancelled){setUser(null);setNotifications([]);setShowNotifications(false);setAuth(true)}};window.addEventListener('porsball:auth-expired',expired);if(!localStorage.getItem('porsball_token'))return()=>window.removeEventListener('porsball:auth-expired',expired);const loadNotifications=()=>{api.notifications().then(x=>{if(!cancelled)setNotifications(x)}).catch(()=>{});};api.me().then(x=>{if(!cancelled)setUser(x)}).catch(()=>localStorage.removeItem('porsball_token'));loadNotifications();const timer=window.setInterval(loadNotifications,30000);const onFocus=()=>loadNotifications();window.addEventListener('focus',onFocus);return()=>{cancelled=true;window.clearInterval(timer);window.removeEventListener('focus',onFocus);window.removeEventListener('porsball:auth-expired',expired)}},[])
 const nav=(x:string)=>setTab(x)
 if(splitPath)return <SplitGuestPage token={splitPath[1]}/>
 return <div className="app"><header><div className="brand"><b>Pors</b><span>Ball</span> โฝ</div><div className="location">๐“ เธเธฃเธธเธเน€เธ—เธเธกเธซเธฒเธเธเธฃ, เธเธฃเธฐเน€เธ—เธจเนเธ—เธข</div><div className="header-actions">{user&&<button type="button" className="account-chip" onClick={()=>nav('profile')} aria-label="เน€เธเธดเธ”เนเธเธฃเนเธเธฅเนเธเธญเธเธเธฑเธ"><span className="avatar mini-avatar">{user.avatar?<img src={user.avatar} alt=""/>:user.name.charAt(0).toUpperCase()}</span><span>{user.name}</span></button>}<div style={{position:'relative'}}><button type="button" className="login" aria-expanded={user?showNotifications:false} aria-label={user?'เธเธฒเธฃเนเธเนเธเน€เธ•เธทเธญเธ':'เน€เธเนเธฒเธชเธนเนเธฃเธฐเธเธ'} onClick={()=>user?setShowNotifications(!showNotifications):setAuth(true)}>{user?'๐””'+(notifications.filter(n=>!n.read).length?` ${notifications.filter(n=>!n.read).length}`:''):'เน€เธเนเธฒเธชเธนเนเธฃเธฐเธเธ'}</button>{showNotifications&&user&&<NotificationPanel items={notifications} setItems={setNotifications} close={()=>setShowNotifications(false)}/>}</div></div></header><main>
 {tab==='home'&&<Home query={query} setQuery={setQuery} venues={venues} matches={matches} select={setSelected} nav={nav} venueError={venueError} retry={loadVenues}/>} {tab==='matches'&&<Matches matches={matches} refresh={refreshMatches} user={user}/>} {tab==='profile'&&<Profile user={user} onLogout={()=>{localStorage.removeItem('porsball_token');setUser(null);setNotifications([]);setShowNotifications(false);setTab('home')}}/>} {tab==='bookings'&&<Bookings/>} {tab==='manage'&&(user?.role==='admin'?<AdminDashboard currentUserId={user.id}/>:user?.role==='owner'?<><OwnerDashboard/><OwnerBookings/></>:<Manage user={user}/>)} {tab==='create'&&<CreateMatchPage user={user}/>}
 </main>{selected&&<Booking v={selected} close={()=>setSelected(null)}/>} {auth&&<Auth close={()=>setAuth(false)} onUser={setUser}/>}
 <nav className="bottom"><Nav active={tab==='home'} icon="โ" text="เธซเธเนเธฒเนเธฃเธ" click={()=>nav('home')}/><Nav active={tab==='matches'} icon="โฝ" text="เธซเธฒเน€เธเธทเนเธญเธ" click={()=>nav('matches')}/>{(user?.role==='owner'||user?.role==='admin')&&<Nav active={tab==='manage'} icon={String.fromCodePoint(9881)} text={user?.role==='admin'?'เนเธญเธ”เธกเธดเธ':'เธเธฑเธ”เธเธฒเธฃเธชเธเธฒเธก'} click={()=>nav('manage')}/>}<Nav active={tab==='bookings'} icon="๐“…" text="เธเธฒเธฃเธเธญเธ" click={()=>nav('bookings')}/><Nav active={tab==='profile'} icon="๐‘ค" text="เนเธเธฃเนเธเธฅเน" click={()=>nav('profile')}/></nav></div>
}
function NotificationPanel({items,setItems,close}:{items:Notification[];setItems:(x:Notification[])=>void;close:()=>void}){const unread=items.filter(n=>!n.read);const read=async(id:number)=>{await api.readNotification(id);setItems(items.map(n=>n.id===id?{...n,read:1}:n))};const all=async()=>{await api.readAllNotifications();setItems(items.map(n=>({...n,read:1})))};useEffect(()=>{const onKey=(e:globalThis.KeyboardEvent)=>{if(e.key==='Escape')close()};document.addEventListener('keydown',onKey);return()=>document.removeEventListener('keydown',onKey)},[close]);return <div className="notification-panel" role="dialog" aria-modal="true" aria-label="เธเธฒเธฃเนเธเนเธเน€เธ•เธทเธญเธ"><div className="notification-head"><b>เธเธฒเธฃเนเธเนเธเน€เธ•เธทเธญเธ</b>{unread.length>0&&<button type="button" onClick={all}>เธญเนเธฒเธเธ—เธฑเนเธเธซเธกเธ”</button>}<button type="button" onClick={close} aria-label="เธเธดเธ”เธเธฒเธฃเนเธเนเธเน€เธ•เธทเธญเธ" title="เธเธดเธ”">ร—</button></div>{items.length?items.map(n=><button className={n.read?'notification read':'notification'} key={n.id} onClick={()=>!n.read&&read(n.id)}><b>{n.title}</b><span>{n.message}</span><small>{new Date(n.created_at+'Z').toLocaleString('th-TH')}</small></button>):<div className="notification-empty">เธขเธฑเธเนเธกเนเธกเธตเธเธฒเธฃเนเธเนเธเน€เธ•เธทเธญเธ</div>}</div>}
function Nav(p:{active:boolean;icon:string;text:string;click:()=>void}){return <button type="button" className={p.active?'navitem active':'navitem'} onClick={p.click}><strong>{p.icon}</strong><span>{p.text}</span></button>}
function Home({query,setQuery,venues,matches,select,nav,venueError,retry}:HomeProps){const [filter,setFilter]=useState('all');const filtered=venues.filter(v=>filter==='all'||(filter==='roof'&&!!v.roof)||(filter==='5v5'&&/5v5/i.test(v.field_types))||(filter==='7v7'&&/7v7/i.test(v.field_types))||(filter==='11v11'&&/11v11/i.test(v.field_types)));return <><section className="hero"><div><p className="eyebrow">โฝ เธเธญเธเธชเธเธฒเธกเนเธเธเน€เธฃเธตเธขเธฅเนเธ—เธกเน</p><h1>เธงเธฑเธเธเธตเนเธเธฐเน€เธ•เธฐเธ—เธตเนเนเธซเธเธ”เธต?</h1><p className="sub">เธเนเธเธซเธฒเธชเธเธฒเธกเนเธเธฅเนเธเธธเธ“ เน€เธเธดเธ”เธเธฑเธ”เธซเธฒเน€เธเธทเนเธญเธ เนเธฅเธฐเธซเธฒเธฃเธเนเธฒเธเธญเธฅเนเธซเนเธเธเนเธเธ—เธตเนเน€เธ”เธตเธขเธง</p><div className="search"><span>โ•</span><input value={query} onChange={e=>setQuery(e.target.value)} placeholder="เธเนเธเธซเธฒเธชเธเธฒเธก, เนเธเธ, เธซเธฃเธทเธญเธชเธ–เธฒเธเธ—เธตเน"/><button>เธเนเธเธซเธฒ</button></div></div><div className="hero-ball">โฝ</div></section><section><div className="section-head"><h2>เธชเธเธฒเธกเนเธเธฅเนเธเธธเธ“</h2></div><div className="chips"><button type="button" className={filter==='all'?'active':''} onClick={()=>setFilter('all')}>เธ—เธฑเนเธเธซเธกเธ”</button><button type="button" className={filter==='5v5'?'active':''} onClick={()=>setFilter('5v5')}>5v5</button><button type="button" className={filter==='7v7'?'active':''} onClick={()=>setFilter('7v7')}>7v7</button><button type="button" className={filter==='11v11'?'active':''} onClick={()=>setFilter('11v11')}>11v11</button><button type="button" className={filter==='roof'?'active':''} onClick={()=>setFilter('roof')}>เธซเธฅเธฑเธเธเธฒ</button></div><div className="pitch-grid">{filtered.map(v=><PitchCard key={v.id} v={v} click={()=>select(v)}/>)}</div>{!venueError&&venues.length>0&&!filtered.length&&<div className="panel">เนเธกเนเธเธเธชเธเธฒเธกเธ—เธตเนเธ•เธฃเธเธเธฑเธเธ•เธฑเธงเธเธฃเธญเธเธเธตเน เธฅเธญเธเน€เธฅเธทเธญเธเธ•เธฑเธงเธเธฃเธญเธเธญเธทเนเธ</div>}{venueError?<div className="panel"><b>เนเธกเนเธชเธฒเธกเธฒเธฃเธ–เน€เธเธทเนเธญเธกเธ•เนเธญเน€เธเธดเธฃเนเธเน€เธงเธญเธฃเน</b><p>เธเธฃเธธเธ“เธฒเธ•เธฃเธงเธเธชเธญเธเธเธฒเธฃเน€เธเธทเนเธญเธกเธ•เนเธญ เนเธฅเนเธงเธฅเธญเธเนเธซเธกเนเธญเธตเธเธเธฃเธฑเนเธ</p><button type="button" className="confirm" onClick={retry}>เธฅเธญเธเธญเธตเธเธเธฃเธฑเนเธ</button></div>:!venues.length&&<div className="panel">เนเธกเนเธเธเธชเธเธฒเธกเธ—เธตเนเธเนเธเธซเธฒ</div>}</section><section className="match-section"><div className="section-head"><div><h2>โฝ เธเธฑเธ”เธ—เธตเนเธเธณเธฅเธฑเธเธซเธฒเธเธ</h2><p>เธเนเธญเธกเธนเธฅเธเธฒเธเธฃเธฐเธเธเธเธฃเธดเธ</p></div><button type="button" className="link" onClick={()=>nav('matches')}>เธ”เธนเธ—เธฑเนเธเธซเธกเธ” โ’</button></div><div className="match-grid">{matches.length?matches.slice(0,3).map(m=><MatchCard key={m.id} m={m}/>):<div className="panel home-empty-match"><b>เธขเธฑเธเนเธกเนเธกเธตเธเธฑเธ”เธ—เธตเนเน€เธเธดเธ”เธฃเธฑเธ</b><p>เน€เธกเธทเนเธญเธกเธตเธเธเน€เธเธดเธ”เธเธฑเธ”เธซเธฒเน€เธเธทเนเธญเธ เธเธฑเธ”เธเธฐเนเธชเธ”เธเธ—เธตเนเธเธตเน</p></div>}</div></section></>}
function PitchCard({v,click}:{v:Venue;click:()=>void}){return <article className="pitch-card" role="button" tabIndex={0} aria-label={`เธ”เธนเธฃเธญเธเธงเนเธฒเธ ${v.name}`} onClick={click} onKeyDown={e=>{if(e.key==='Enter'||e.key===' '){e.preventDefault();click()}}}><div className="pitch-photo"><span>โฝ</span><small>โญ</small></div><div className="pitch-body"><div className="rating">โ… {v.rating.toFixed(1)}</div><h3>{v.name}</h3><p>{v.area}  โ€ข  {v.field_types}</p><div className="card-foot"><b>เธฟ{v.price_per_hour.toLocaleString()}<em>/เธเธก.</em></b><button type="button" onClick={e=>{e.stopPropagation();click()}}>เธ”เธนเธฃเธญเธเธงเนเธฒเธ</button></div></div></article>}
function localDateTime(date:string,time:string){return new Date(`${date}T${time}:00+07:00`)}
function todayISO(){const parts=new Intl.DateTimeFormat('en-US',{timeZone:'Asia/Bangkok',year:'numeric',month:'2-digit',day:'2-digit'}).formatToParts(new Date());const y=parts.find(p=>p.type==='year')?.value||'';const m=parts.find(p=>p.type==='month')?.value||'';const d=parts.find(p=>p.type==='day')?.value||'';return `${y}-${m}-${d}`}
function bookingState(date:string,start:string,end:string){const now=new Date(),from=localDateTime(date,start),to=localDateTime(date,end);if(now>=to)return 'expired';if(now>=from)return 'active';return 'upcoming'}
function MatchCard({m}:{m:Match}){const end=m.end_time||m.start_time;const state=bookingState(m.match_date,m.start_time,end);return <article className="match-card"><div className="match-icon">โฝ</div><div><h3>{m.title}</h3><p>{m.match_date}  โ€ข  {m.start_time}โ€“{end}  โ€ข  {m.venue_name}</p><div className="avatars">๐‘ฅ {m.players}/{m.max_players} เธเธ</div></div><b>{state==='expired'?'เธซเธกเธ”เน€เธงเธฅเธฒ':state==='active'?'เธเธณเธฅเธฑเธเนเธเนเธเธฒเธ':`เธฟ${m.fee}/เธเธ`}</b></article>}
function Booking({v,close}:{v:Venue;close:()=>void}){const [date,setDate]=useState(todayISO()),[slots,setSlots]=useState<Slot[]>([]),[slot,setSlot]=useState(''),[busy,setBusy]=useState(false),[error,setError]=useState('');useEffect(()=>{setSlot('');setError('');api.slots(v.id,date).then(x=>{setSlots(x.slots);const now=new Date();const first=x.slots.find(s=>s.available&&localDateTime(date,s.end)>now);setSlot(first?.start||'')}).catch(()=>{setSlots([]);setError('เนเธกเนเธชเธฒเธกเธฒเธฃเธ–เนเธซเธฅเธ”เธฃเธญเธเธชเธเธฒเธกเนเธ”เน เธเธฃเธธเธ“เธฒเธฅเธญเธเนเธซเธกเนเธญเธตเธเธเธฃเธฑเนเธ')})},[v.id,date]);const confirm=async()=>{if(!slot){setError('เธเธฃเธธเธ“เธฒเน€เธฅเธทเธญเธเน€เธงเธฅเธฒเธ—เธตเนเธ•เนเธญเธเธเธฒเธฃเธเธญเธ');return}if(!localStorage.getItem('porsball_token')){setError('เธเธฃเธธเธ“เธฒเน€เธเนเธฒเธชเธนเนเธฃเธฐเธเธเธเนเธญเธเธเธญเธเธชเธเธฒเธก');return}const selectedSlot=slots.find(s=>s.start===slot);if(!selectedSlot){setError('เธฃเธญเธเน€เธงเธฅเธฒเธเธตเนเนเธกเนเธเธฃเนเธญเธกเนเธเนเธเธฒเธ เธเธฃเธธเธ“เธฒเน€เธฅเธทเธญเธเนเธซเธกเน');setSlot('');return}const chosenEnd=localDateTime(date,selectedSlot.end);if(chosenEnd<=new Date()){setSlot('');setError('เน€เธงเธฅเธฒเธเธตเนเน€เธฅเธขเน€เธงเธฅเธฒเธเธญเธเนเธฅเนเธง เธเธฃเธธเธ“เธฒเน€เธฅเธทเธญเธเน€เธงเธฅเธฒเนเธซเธกเน');return}setBusy(true);try{await api.book(v.id,date,selectedSlot.start,selectedSlot.end,v.price_per_hour);window.dispatchEvent(new Event('porsball:data-changed'));close()}catch(e){setError(e instanceof Error?e.message:'เธเธญเธเนเธกเนเธชเธณเน€เธฃเนเธ')}finally{setBusy(false)}};return <div className="modal"><div className="booking"><button className="close" onClick={close} aria-label="เธเธดเธ”เธซเธเนเธฒเธ•เนเธฒเธเธเธญเธเธชเธเธฒเธก" title="เธเธดเธ”">ร—</button><div className="booking-photo">โฝ</div><div className="booking-content"><div className="rating">โ… {v.rating.toFixed(1)}</div><h2>{v.name}</h2><p>{v.area}  โ€ข  {v.field_types}{v.roof?'  โ€ข  เธซเธฅเธฑเธเธเธฒ':''}</p><h3>เน€เธฅเธทเธญเธเธงเธฑเธเธ—เธตเน</h3><input type="date" min={todayISO()} value={date} onChange={e=>setDate(e.target.value)}/><h3>เน€เธฅเธทเธญเธเน€เธงเธฅเธฒ</h3><div className="slots">{slots.map(s=>{const slotStart=localDateTime(date,s.start),past=slotStart<=new Date(),canBook=s.available&&!past;return <button type="button" disabled={!canBook} className={slot===s.start?'selected':''} onClick={()=>canBook&&setSlot(s.start)} key={s.start}>{s.start}<small>{past?'เน€เธฅเธขเน€เธงเธฅเธฒเธเธญเธ':s.available?'เธงเนเธฒเธ':'เน€เธ•เนเธก'}</small></button>})}</div>{error&&<div className="auth-error" role="alert">{error}</div>}<div className="summary"><span>{slot||'เธขเธฑเธเนเธกเนเนเธ”เนเน€เธฅเธทเธญเธเน€เธงเธฅเธฒ'}</span><b>เธฟ{v.price_per_hour.toLocaleString()}</b></div><button type="button" className="confirm" disabled={!slot||busy} onClick={confirm}>{busy?'เธเธณเธฅเธฑเธเธเธญเธ...':'เธขเธทเธเธขเธฑเธเธเธฒเธฃเธเธญเธ โ’'}</button></div></div></div>}
function Matches({matches,refresh,user}:{matches:Match[];refresh:()=>void;user:User|null}){return <section className="page"><p className="eyebrow">COMMUNITY</p><h1>เธซเธฒเน€เธเธทเนเธญเธเธกเธฒเน€เธ•เธฐเธ”เนเธงเธขเธเธฑเธ</h1><p>เน€เธฅเธทเธญเธเธเธฑเธ”เธเธฒเธเธเนเธญเธกเธนเธฅเนเธเธฃเธฐเธเธ เนเธฅเนเธงเธเธ”เน€เธเนเธฒเธฃเนเธงเธก</p>{matches.length?matches.map(m=><MatchJoin key={m.id} m={m} refresh={refresh} user={user}/>):<div className="panel"><b>เธ•เธญเธเธเธตเนเธขเธฑเธเนเธกเนเธกเธตเธเธฑเธ”เธ—เธตเนเน€เธเธดเธ”เธฃเธฑเธ</b><p>เธฅเธญเธเธเธฅเธฑเธเธกเธฒเนเธซเธกเนเธญเธตเธเธเธฃเธฑเนเธ เธซเธฃเธทเธญเธชเธฃเนเธฒเธเธเธฑเธ”เธเธฒเธเธเธฒเธฃเธเธญเธเธชเธเธฒเธกเธเธญเธเธเธธเธ“</p></div>}</section>}
function MatchJoin({m,refresh,user}:{m:Match;refresh:()=>void;user:User|null}){
 const [busy,setBusy]=useState(false),[players,setPlayers]=useState<MatchPlayer[]>([]),[show,setShow]=useState(false),[status,setStatus]=useState('')
 const isOwner=!!user&&user.id===m.creator_id
 const end=m.end_time||m.start_time
 const state=bookingState(m.match_date,m.start_time,end)
 const loadPlayers=async()=>{try{const r=await api.players(m.id);setPlayers(r.players)}catch{setPlayers([])}}
 useEffect(()=>{let cancelled=false;if(!user){setPlayers([]);return()=>{cancelled=true}};api.players(m.id).then(r=>{if(!cancelled)setPlayers(r.players)}).catch(()=>{if(!cancelled)setPlayers([])});return()=>{cancelled=true}},[m.id,user])
 const join=async()=>{if(!localStorage.getItem('porsball_token')){setStatus('เธเธฃเธธเธ“เธฒเน€เธเนเธฒเธชเธนเนเธฃเธฐเธเธเธเนเธญเธเน€เธเนเธฒเธฃเนเธงเธกเธเธฑเธ”');return}setStatus('');setBusy(true);try{await api.join(m.id);await loadPlayers();refresh();setStatus('เน€เธเนเธฒเธฃเนเธงเธกเธเธฑเธ”เนเธฅเนเธง โ“')}catch(e){setStatus(e instanceof Error?e.message:'เน€เธเนเธฒเธฃเนเธงเธกเนเธกเนเธชเธณเน€เธฃเนเธ')}finally{setBusy(false)}}
 const leave=async()=>{if(!confirm('เธขเธทเธเธขเธฑเธเธญเธญเธเธเธฒเธเธเธฑเธ”เธเธตเน?'))return;setStatus('');setBusy(true);try{await api.leaveMatch(m.id);await loadPlayers();refresh();setStatus('เธญเธญเธเธเธฒเธเธเธฑเธ”เนเธฅเนเธง โ“')}catch(e){setStatus(e instanceof Error?e.message:'เธญเธญเธเธเธฒเธเธเธฑเธ”เนเธกเนเธชเธณเน€เธฃเนเธ')}finally{setBusy(false)}}
 const remove=async(id:number)=>{if(!confirm('เธเธณเธชเธกเธฒเธเธดเธเธเธเธเธตเนเธญเธญเธเธเธฒเธเธเธฑเธ”?'))return;setStatus('');setBusy(true);try{await api.removePlayer(m.id,id);await loadPlayers();refresh();setStatus('เธเธณเธชเธกเธฒเธเธดเธเธญเธญเธเธเธฒเธเธเธฑเธ”เนเธฅเนเธง โ“')}catch(e){setStatus(e instanceof Error?e.message:'เธเธณเธชเธกเธฒเธเธดเธเธญเธญเธเนเธกเนเธชเธณเน€เธฃเนเธ')}finally{setBusy(false)}}
 return <article className="match-card" style={{marginBottom:12}}><div className="match-icon">โฝ</div><div style={{flex:1}}><h3>{m.title}</h3><p>{m.match_date}  โ€ข  {m.start_time}  โ€ข  {m.venue_name}</p><div className="avatars">๐‘ฅ {m.players}/{m.max_players} เธเธ</div>{status&&<div className="auth-error" role="status" style={{marginTop:8}}>{status}</div>}{show&&<div id={`match-members-${m.id}`} className="panel" role="region" aria-label="เธชเธกเธฒเธเธดเธเนเธเธเธฑเธ”" style={{marginTop:8}}><b>เธชเธกเธฒเธเธดเธเนเธเธเธฑเธ”</b>{players.map(p=><div key={p.id} style={{display:'flex',justifyContent:'space-between',gap:8,padding:'6px 0'}}><span>{p.name}{p.owner?' (เน€เธเนเธฒเธเธญเธ)':''}</span>{isOwner&&!p.owner&&<button type="button" onClick={()=>remove(p.id)} disabled={busy}>เธเธณเธญเธญเธ</button>}</div>)}</div>}</div><div><button type="button" aria-expanded={show} aria-controls={`match-members-${m.id}`} aria-label={show?'เธเนเธญเธเธชเธกเธฒเธเธดเธเนเธเธเธฑเธ”':'เธ”เธนเธชเธกเธฒเธเธดเธเนเธเธเธฑเธ”'} onClick={()=>{setShow(!show);if(!show)void loadPlayers()}}>เธชเธกเธฒเธเธดเธ</button>{isOwner?<button type="button" disabled aria-label="เธเธธเธ“เน€เธเนเธเน€เธเนเธฒเธเธญเธเธเธฑเธ”">เธเธธเธ“เน€เธเนเธเน€เธเนเธฒเธเธญเธเธเธฑเธ”</button>:players.some(p=>user&&p.id===user.id)?<button type="button" disabled={busy} onClick={leave}>เธญเธญเธเธเธฒเธเธเธฑเธ”</button>:<button type="button" disabled={busy||state==='expired'||m.players>=m.max_players||!m.open_for_join} onClick={join}>{state==='expired'?'เธซเธกเธ”เน€เธงเธฅเธฒ':m.players>=m.max_players?'เน€เธ•เนเธก':!m.open_for_join?'เธเธดเธ”เธฃเธฑเธเธเธ':'เน€เธเนเธฒเธฃเนเธงเธก'}</button>}</div></article>
}
function CreateMatchPage({user}:{user:User|null}){const [rows,setRows]=useState<BookingRow[]>([]),[selected,setSelected]=useState<BookingRow|null>(null);useEffect(()=>{if(!user)return;const load=()=>api.bookings().then(setRows).catch(()=>setRows([]));void load();const onChanged=()=>void load();window.addEventListener('porsball:data-changed',onChanged);return()=>window.removeEventListener('porsball:data-changed',onChanged)},[user]);if(!user)return <section className="page"><p className="eyebrow">CREATE MATCH</p><h1>เธชเธฃเนเธฒเธเธเธฑเธ”</h1><div className="panel">เธเธฃเธธเธ“เธฒเน€เธเนเธฒเธชเธนเนเธฃเธฐเธเธเธเนเธญเธ เน€เธเธทเนเธญเธชเธฃเนเธฒเธเธเธฑเธ”เธเธฒเธเธชเธเธฒเธกเธ—เธตเนเธเธธเธ“เธเธญเธ</div></section>;return <section className="page"><p className="eyebrow">CREATE MATCH</p><h1>เธชเธฃเนเธฒเธเธเธฑเธ”</h1><p>เน€เธฅเธทเธญเธเธชเธเธฒเธกเนเธฅเธฐเน€เธงเธฅเธฒเธ—เธตเนเธเธธเธ“เธเธญเธเนเธงเน เนเธฅเนเธงเน€เธเธดเธ”เธฃเธฑเธเน€เธเธทเนเธญเธเนเธ”เนเธ—เธฑเธเธ—เธต</p>{rows.length?rows.map(r=>{const state=bookingState(r.booking_date,r.start_time,r.end_time);return <div className="panel booking-row" key={r.id}><div><h3>{r.venue_name}</h3><p>{r.booking_date}  โ€ข  {r.start_time}โ€“{r.end_time}</p><b>เธฟ{r.total_price.toLocaleString()}</b>{state==='expired'&&<strong className="expired-label">เน€เธฅเธขเน€เธงเธฅเธฒเธเธญเธ</strong>}</div><button className="primary" disabled={state==='expired'} onClick={()=>setSelected(r)}>{state==='expired'?'เน€เธฅเธขเน€เธงเธฅเธฒเธเธญเธ':'เธชเธฃเนเธฒเธเธเธฑเธ”เธเธฒเธเธเธฒเธฃเธเธญเธเธเธตเน'}</button></div>}):<div className="panel">เธขเธฑเธเนเธกเนเธกเธตเธเธฒเธฃเธเธญเธเธชเธเธฒเธก เธเธฃเธธเธ“เธฒเธเธญเธเธชเธเธฒเธกเธเนเธญเธเธชเธฃเนเธฒเธเธเธฑเธ”</div>}{selected&&<CreateMatch booking={selected} close={()=>setSelected(null)} done={()=>{setSelected(null);api.bookings().then(setRows).catch(()=>setRows([]))}}/>}</section>}
function CreateMatch({booking,close,done}:{booking:BookingRow;close:()=>void;done:()=>void}){const [title,setTitle]=useState('Friendly Match'),[maxPlayers,setMaxPlayers]=useState(10),[busy,setBusy]=useState(false),[error,setError]=useState('');const total=booking.total_price;const fee=Math.ceil(total/Math.max(1,maxPlayers));const submit=async()=>{setError('');const cleanTitle=title.trim();if(!cleanTitle){setError('เธเธฃเธธเธ“เธฒเธฃเธฐเธเธธเธเธทเนเธญเธเธฑเธ”');return}if(maxPlayers<2||maxPlayers>30){setError('เธเธณเธเธงเธเธเธนเนเน€เธฅเนเธเธ•เนเธญเธเธญเธขเธนเนเธฃเธฐเธซเธงเนเธฒเธ 2โ€“30 เธเธ');return}setBusy(true);try{await api.createMatch(booking.id,cleanTitle,fee,maxPlayers);window.dispatchEvent(new Event('porsball:data-changed'));done()}catch(e){setError(e instanceof Error?e.message:'เน€เธเธดเธ”เธซเธฒเธเธเนเธกเนเธชเธณเน€เธฃเนเธ')}finally{setBusy(false)}};return <div className="auth-overlay"><div className="auth-box"><button type="button" className="auth-x" onClick={close} aria-label="เธเธดเธ”เธซเธเนเธฒเธ•เนเธฒเธ" title="เธเธดเธ”">ร—</button><h2>เน€เธเธดเธ”เธซเธฒเธเธ</h2><p>{booking.venue_name}  โ€ข  {booking.booking_date}  โ€ข  {booking.start_time}โ€“{booking.end_time}</p><input value={title} onChange={e=>setTitle(e.target.value)} placeholder="เธเธทเนเธญเธเธฑเธ”"/><label>เธเธณเธเธงเธเธเธนเนเน€เธฅเนเธเธ—เธฑเนเธเธซเธกเธ”<input type="number" min="2" max="30" value={maxPlayers} onChange={e=>setMaxPlayers(Math.max(2,Math.min(30,Number(e.target.value)||2)))}/></label><p>เธเนเธฒเธชเธเธฒเธก เธฟ{total.toLocaleString()}  โ€ข  เธเธเธฅเธฐเธเธฃเธฐเธกเธฒเธ“ เธฟ{fee.toLocaleString()}</p><div className="auth-error">{error}</div><button type="button" className="auth-submit" disabled={busy} onClick={submit}>{busy?'เธเธณเธฅเธฑเธเน€เธเธดเธ”...':'เน€เธเธดเธ”เธซเธฒเธเธ'}</button></div></div>}
function Profile({user,onLogout}:{user:User|null;onLogout:()=>void}){const [editing,setEditing]=useState(false),[name,setName]=useState(user?.name||''),[phone,setPhone]=useState(user?.phone||''),[address,setAddress]=useState(user?.address||''),[avatar,setAvatar]=useState<string|null>(user?.avatar||null),[busy,setBusy]=useState(false),[error,setError]=useState('');useEffect(()=>{setName(user?.name||'');setPhone(user?.phone||'');setAddress(user?.address||'');setAvatar(user?.avatar||null)},[user]);const pickPhoto=(e:React.ChangeEvent<HTMLInputElement>)=>{const file=e.target.files?.[0];if(!file)return;if(!/^image\/(png|jpe?g|webp)$/i.test(file.type)){setError('รองรับรูป PNG, JPG หรือ WebP');return}if(file.size>3*1024*1024){setError('รูปต้องมีขนาดไม่เกิน 3 MB');return}const img=new Image(),reader=new FileReader();reader.onload=()=>{img.onload=()=>{const max=480,scale=Math.min(1,max/Math.max(img.width,img.height)),canvas=document.createElement('canvas');canvas.width=Math.max(1,Math.round(img.width*scale));canvas.height=Math.max(1,Math.round(img.height*scale));canvas.getContext('2d')?.drawImage(img,0,0,canvas.width,canvas.height);setAvatar(canvas.toDataURL('image/webp',0.78));setError('')};img.src=String(reader.result)};reader.readAsDataURL(file)};const save=async()=>{setError('');if(!name.trim()||phone.trim().length<8||!address.trim()){setError('กรุณากรอกชื่อ เบอร์โทร และที่อยู่ให้ครบ');return}setBusy(true);try{const u=await api.updateProfile({name:name.trim(),phone:phone.trim(),address:address.trim(),avatar});setEditing(false);window.dispatchEvent(new CustomEvent('porsball:user-updated',{detail:u}))}catch(e){setError(e instanceof Error?e.message:'บันทึกโปรไฟล์ไม่สำเร็จ')}finally{setBusy(false)}};if(!user)return <section className="page"><h1>โปรไฟล์</h1><div className="panel">กรุณาเข้าสู่ระบบเพื่อดูโปรไฟล์</div></section>;const roleLabel=user.role==='admin'?'แอดมิน':user.role==='owner'?'เจ้าของสนาม':'ผู้ใช้';return <section className="page"><div className="profile-head"><div className="avatar profile-avatar">{avatar?<img src={avatar} alt="รูปโปรไฟล์"/>:user.name.charAt(0).toUpperCase()}</div><div style={{flex:1}}><p className="eyebrow">PROFILE</p><h1>{user.name}</h1><p>{user.email}</p><p>{user.phone||'ยังไม่ได้เพิ่มเบอร์โทร'} · {user.address||'ยังไม่ได้เพิ่มที่อยู่'}</p></div><button type="button" className="primary" onClick={()=>{setEditing(true);setError('')}}>แก้ไขโปรไฟล์</button><button type="button" onClick={onLogout}>ออกจากระบบ</button></div>{editing&&<div className="panel profile-edit"><h2>แก้ไขโปรไฟล์</h2><label>ชื่อ<input value={name} onChange={e=>setName(e.target.value)} maxLength={80}/></label><label>เบอร์โทร<input value={phone} onChange={e=>setPhone(e.target.value)} inputMode="tel" maxLength={20}/></label><label>ที่อยู่<textarea value={address} onChange={e=>setAddress(e.target.value)} maxLength={300} rows={3}/></label><label>รูปโปรไฟล์<input type="file" accept="image/png,image/jpeg,image/webp" onChange={pickPhoto}/></label>{avatar&&<button type="button" onClick={()=>setAvatar(null)}>ลบรูปโปรไฟล์</button>}{error&&<div className="auth-error" role="alert">{error}</div>}<div className="manage-actions"><button type="button" className="primary" disabled={busy} onClick={save}>{busy?'กำลังบันทึก...':'บันทึก'}</button><button type="button" onClick={()=>setEditing(false)}>ยกเลิก</button></div></div>}<div className="stats"><div><b>{user.wins+user.losses}</b><span>นัด</span></div><div><b>{user.wins}</b><span>ชนะ</span></div><div><b>{user.points}</b><span>แต้ม</span></div><div><b>{roleLabel}</b><span>บทบาท</span></div></div></section>}
function Bookings(){const [rows,setRows]=useState<BookingRow[]|null>(null),[splitFor,setSplitFor]=useState<BookingRow|null>(null),[openFor,setOpenFor]=useState<BookingRow|null>(null),[busy,setBusy]=useState<number|null>(null),[matchBusy,setMatchBusy]=useState<number|null>(null),[status,setStatus]=useState('');
const load=()=>api.bookings().then(setRows).catch(()=>setRows([]));
useEffect(()=>{void load();const onChanged=()=>void load();window.addEventListener('porsball:data-changed',onChanged);return()=>window.removeEventListener('porsball:data-changed',onChanged)},[]);
const cancel=async(id:number)=>{if(!confirm('เธขเธทเธเธขเธฑเธเธขเธเน€เธฅเธดเธเธเธฒเธฃเธเธญเธเธชเธเธฒเธกเธเธตเน?'))return;setStatus('');setBusy(id);try{await api.cancelBooking(id);await load();window.dispatchEvent(new Event('porsball:data-changed'));setStatus('เธขเธเน€เธฅเธดเธเธเธฒเธฃเธเธญเธเธชเธเธฒเธกเนเธฅเนเธง โ“')}catch(e){setStatus(e instanceof Error?e.message:'เธขเธเน€เธฅเธดเธเธเธฒเธฃเธเธญเธเนเธกเนเธชเธณเน€เธฃเนเธ')}finally{setBusy(null)}};
const toggleMatch=async(r:BookingRow)=>{if(!r.match_id)return;setStatus('');setMatchBusy(r.id);try{if(r.open_for_join){await api.closeMatch(r.match_id);setStatus('เธเธดเธ”เธฃเธฑเธเธเธเนเธเธเธฑเธ”เนเธฅเนเธง โ“')}else{await api.openMatch(r.match_id);setStatus('เน€เธเธดเธ”เธฃเธฑเธเธเธเนเธเธเธฑเธ”เนเธฅเนเธง โ“')}await load();window.dispatchEvent(new Event('porsball:data-changed'))}catch(e){setStatus(e instanceof Error?e.message:'เน€เธเธฅเธตเนเธขเธเธชเธ–เธฒเธเธฐเธเธฑเธ”เนเธกเนเธชเธณเน€เธฃเนเธ')}finally{setMatchBusy(null)}};
return <section className="page"><p className="eyebrow">MY BOOKINGS</p><h1>เธเธฒเธฃเธเธญเธเธเธญเธเธเธฑเธ</h1>{status&&<div className="auth-error" role="status" style={{marginBottom:12}}>{status}</div>}{rows===null?<div className="panel">เธเธณเธฅเธฑเธเนเธซเธฅเธ”...</div>:rows.length?rows.map(r=>{const state=bookingState(r.booking_date,r.start_time,r.end_time);const hasMatch=!!r.match_id;return <div className="panel booking-row" key={r.id}><div><h3>{r.venue_name}</h3><p>{r.booking_date}  โ€ข  {r.start_time}โ€“{r.end_time}</p><b>เธฟ{r.total_price.toLocaleString()}</b>{state==='expired'&&<strong className="expired-label">เน€เธฅเธขเน€เธงเธฅเธฒเธเธญเธ</strong>}{state==='active'&&<strong className="active-label">เธเธณเธฅเธฑเธเนเธเนเธเธฒเธ</strong>}{hasMatch&&state==='upcoming'&&<strong className={r.open_for_join?'active-label':'expired-label'}>{r.open_for_join?'เน€เธเธดเธ”เธฃเธฑเธเธเธเธญเธขเธนเน':'เธเธดเธ”เธฃเธฑเธเธเธเนเธฅเนเธง'}</strong>}</div><div className="manage-actions">{state==='upcoming'&&(hasMatch?<button type="button" className="primary" disabled={matchBusy===r.id} onClick={()=>toggleMatch(r)}>{matchBusy===r.id?'เธเธณเธฅเธฑเธเน€เธเธฅเธตเนเธขเธ...':r.open_for_join?'เธเธดเธ”เธฃเธฑเธเธเธ':'เน€เธเธดเธ”เธฃเธฑเธเธเธ'}</button>:<button type="button" className="primary" onClick={()=>setOpenFor(r)}>เน€เธเธดเธ”เธซเธฒเธเธ</button>)}<button type="button" className="primary" disabled={state==='expired'} onClick={()=>setSplitFor(r)} aria-label={`เธซเธฒเธฃเธเนเธฒเนเธเนเธเนเธฒเธข ${r.venue_name}`}>เธซเธฒเธฃเธเนเธฒเนเธเนเธเนเธฒเธข</button>{state==='upcoming'&&<button type="button" className="danger" disabled={busy===r.id} onClick={()=>cancel(r.id)}>{busy===r.id?'เธเธณเธฅเธฑเธเธขเธเน€เธฅเธดเธ...':'เธขเธเน€เธฅเธดเธเธเธฒเธฃเธเธญเธ'}</button>}</div></div>}):<div className="panel">เธขเธฑเธเนเธกเนเธกเธตเธเธฃเธฐเธงเธฑเธ•เธดเธเธฒเธฃเธเธญเธ</div>}{openFor&&<CreateMatch booking={openFor} close={()=>setOpenFor(null)} done={()=>{setOpenFor(null);load()}}/>}{splitFor&&<SplitModal booking={splitFor} close={()=>setSplitFor(null)} afterClose={()=>{setSplitFor(null);load()}}/>}</section>}
function SplitModal({booking,close,afterClose}:{booking:BookingRow;close:()=>void;afterClose:()=>void}){
 const [count,setCount]=useState(5),[names,setNames]=useState<string[]>(Array(5).fill('')),[bill,setBill]=useState<SplitBill|null>(null),[links,setLinks]=useState<{memberId:number;name:string;url:string}[]>([]),[busy,setBusy]=useState(false),[error,setError]=useState(''),[loading,setLoading]=useState(true)
 useEffect(()=>{api.splitForBooking(booking.id).then(b=>{setBill(b);setCount(b.share_count);setNames(b.members.map(m=>m.name))}).catch(()=>{}).finally(()=>setLoading(false))},[booking.id])
 const resize=(n:number)=>{setCount(n);setNames(prev=>Array.from({length:n},(_,i)=>prev[i]||''))}
 const save=async()=>{setError('');const cleanNames=names.map(n=>n.trim());if(cleanNames.some(n=>!n)){setError('เธเธฃเธธเธ“เธฒเธเธฃเธญเธเธเธทเนเธญเธชเธกเธฒเธเธดเธเนเธซเนเธเธฃเธเธ—เธธเธเธเธ');return}if(new Set(cleanNames.map(n=>n.toLowerCase())).size!==cleanNames.length){setError('เธเธทเนเธญเธชเธกเธฒเธเธดเธเธ•เนเธญเธเนเธกเนเธเนเธณเธเธฑเธ');return}setBusy(true);try{const r=await api.createSplit(booking.id,count,cleanNames);setBill(await api.split(r.id))}catch(e){setError(e instanceof Error?e.message:'เธเธฑเธเธ—เธถเธเธเธดเธฅเนเธกเนเธชเธณเน€เธฃเนเธ')}finally{setBusy(false)}}
 const refresh=async(id:number)=>setBill(await api.split(id))
 const makeLinks=async()=>{if(!bill)return;setBusy(true);setError('');try{const r=await api.shareLinks(bill.id);setLinks(r.links.map(x=>({...x,url:`${window.location.origin}/split/${x.token}`})))}catch(e){setError(e instanceof Error?e.message:'เธชเธฃเนเธฒเธเธฅเธดเธเธเนเนเธเธฃเนเนเธกเนเธชเธณเน€เธฃเนเธ')}finally{setBusy(false)}}
 const copy=async(text:string)=>{try{await navigator.clipboard.writeText(text)}catch{setError('เนเธกเนเธชเธฒเธกเธฒเธฃเธ–เธเธฑเธ”เธฅเธญเธเนเธ”เน เธเธฃเธธเธ“เธฒเธเธฑเธ”เธฅเธญเธเธฅเธดเธเธเนเธ”เนเธงเธขเธ•เธเน€เธญเธ')}}
 const closeBill=async()=>{if(!bill)return;const unpaid=bill.members.filter(m=>!m.paid).length;if(unpaid){setError('เธขเธฑเธเธกเธตเธชเธกเธฒเธเธดเธเธ—เธตเนเธขเธฑเธเนเธกเนเธเนเธฒเธข '+unpaid+' เธเธ');return}setBusy(true);setError('');try{await api.closeSplit(bill.id);window.dispatchEvent(new Event('porsball:data-changed'));afterClose()}catch(e){setError(e instanceof Error?e.message:'เธเธดเธ”เธเธดเธฅเนเธกเนเธชเธณเน€เธฃเนเธ')}finally{setBusy(false)}}
 if(loading)return <div className="auth-overlay"><div className="auth-box split-box"><button type="button" className="auth-x" onClick={close} aria-label="เธเธดเธ”เธซเธเนเธฒเธ•เนเธฒเธ" title="เธเธดเธ”">ร—</button><h2>เธเธณเธฅเธฑเธเนเธซเธฅเธ”เธเธดเธฅ...</h2></div></div>
 return <div className="auth-overlay"><div className="auth-box split-box"><button type="button" className="auth-x" onClick={close} aria-label="เธเธดเธ”เธซเธเนเธฒเธ•เนเธฒเธ" title="เธเธดเธ”">ร—</button><div className="auth-logo"><b>Pors</b><span>Ball</span> โฝ</div><h2>เธซเธฒเธฃเธเนเธฒเนเธเนเธเนเธฒเธข</h2><p>{booking.venue_name}  โ€ข  เธฃเธงเธก เธฟ{booking.total_price.toLocaleString()}</p>
 {!bill?<><label>เธเธณเธเธงเธเธเธ</label><input type="number" min="2" max="30" value={count} onChange={e=>resize(Math.max(2,Math.min(30,Number(e.target.value)||2)))}/><div className="split-names">{names.map((n,i)=><input key={i} value={n} onChange={e=>setNames(prev=>prev.map((x,j)=>j===i?e.target.value:x))} placeholder={'เธเธทเนเธญเธเธเธ—เธตเน '+(i+1)}/>)}</div><div className="split-preview">เธฃเธงเธก เธฟ{booking.total_price.toLocaleString()} โ€ข เน€เธเธฅเธตเนเธขเธเธฃเธฐเธกเธฒเธ“ <b>เธฟ{Math.floor(booking.total_price/count).toLocaleString()}</b></div><div className="auth-error">{error}</div><button type="button" className="auth-submit" disabled={busy} onClick={save}>{busy?'เธเธณเธฅเธฑเธเธเธฑเธเธ—เธถเธ...':'เธชเธฃเนเธฒเธเนเธฅเธฐเธเธฑเธเธ—เธถเธเธเธดเธฅ'}</button></>
 :<><div className="split-preview">เธขเธญเธ”เธ•เนเธญเธเธ <b>เธฟ{bill.members.length?Math.min(...bill.members.map(m=>m.amount)).toLocaleString():bill.share_amount.toLocaleString()}โ€“เธฟ{bill.members.length?Math.max(...bill.members.map(m=>m.amount)).toLocaleString():bill.share_amount.toLocaleString()}</b>  โ€ข  {bill.members.filter(m=>m.paid).length}/{bill.share_count} เธเธเธเนเธฒเธขเนเธฅเนเธง</div><div className="split-members">{bill.members.map(m=><div className="split-member" key={m.id}><span>{m.name} <b>เธฟ{m.amount.toLocaleString()}</b></span><button type="button" disabled={!!m.paid||busy} onClick={async()=>{await api.markPaid(bill.id,m.id);await refresh(bill.id)}}>{m.paid?'เธเนเธฒเธขเนเธฅเนเธง โ“':'เธ—เธณเน€เธเธฃเธทเนเธญเธเธซเธกเธฒเธขเธงเนเธฒเธเนเธฒเธขเนเธฅเนเธง'}</button></div>)}</div><button type="button" className="auth-submit" disabled={busy} onClick={()=>void refresh(bill.id)}>{busy?'เธเธณเธฅเธฑเธเนเธซเธฅเธ”...':'เธฃเธตเน€เธเธฃเธเธชเธ–เธฒเธเธฐเธเธฒเธฃเธเนเธฒเธข'}</button><button type="button" className="auth-submit" disabled={busy||bill.members.some(m=>!m.paid)} onClick={closeBill}>{bill.members.some(m=>!m.paid)?'เธเธดเธ”เธเธดเธฅเนเธ”เนเน€เธกเธทเนเธญเธเนเธฒเธขเธเธฃเธเธ—เธธเธเธเธ':'เธเธดเธ”เธเธดเธฅ โ“'}</button><div className="auth-error">{error}</div><button type="button" className="auth-toggle" disabled={busy} onClick={makeLinks}>{busy?'เธเธณเธฅเธฑเธเธชเธฃเนเธฒเธเธฅเธดเธเธเน...':'เธชเธฃเนเธฒเธเธฅเธดเธเธเนเนเธซเนเธชเธกเธฒเธเธดเธ'}</button>{links.length>0&&<div className="split-members"><b>เธฅเธดเธเธเนเธเธณเธฃเธฐเน€เธเธดเธเนเธขเธเธ•เธฒเธกเธชเธกเธฒเธเธดเธ</b>{links.map(x=><div className="split-member" key={x.memberId}><span>{x.name}</span><button type="button" onClick={()=>void copy(x.url)}>เธเธฑเธ”เธฅเธญเธเธฅเธดเธเธเน</button><small style={{display:'block',wordBreak:'break-all'}}>{x.url}</small></div>)}</div>}</>}
 </div></div>
}
function SplitGuestPage({token}:{token:string}){
 const [bill,setBill]=useState<Awaited<ReturnType<typeof api.splitGuest>>|null>(null),[loading,setLoading]=useState(true),[busy,setBusy]=useState(false),[error,setError]=useState(''),[done,setDone]=useState(false)
 const load=async()=>{try{setBill(await api.splitGuest(token))}catch(e){setBill(null);setError(e instanceof Error?e.message:'เนเธกเนเธเธเธฅเธดเธเธเนเธเธณเธฃเธฐเน€เธเธดเธ')}finally{setLoading(false)}}
 useEffect(()=>{let active=true;api.splitGuest(token).then(data=>{if(active)setBill(data)}).catch(e=>{if(active){setBill(null);setError(e instanceof Error?e.message:'เนเธกเนเธเธเธฅเธดเธเธเนเธเธณเธฃเธฐเน€เธเธดเธ')}}).finally(()=>{if(active)setLoading(false)});return()=>{active=false}},[token])
 const pay=async()=>{setBusy(true);setError('');try{await api.payGuest(token);setDone(true);await load()}catch(e){setError(e instanceof Error?e.message:'เธขเธทเธเธขเธฑเธเธเธฒเธฃเธเนเธฒเธขเนเธกเนเธชเธณเน€เธฃเนเธ')}finally{setBusy(false)}}
 return <div className="app"><main className="page"><div className="auth-box split-box" style={{margin:'40px auto',maxWidth:520}}><div className="auth-logo"><b>Pors</b><span>Ball</span> โฝ</div><h1>เธเธณเธฃเธฐเธเนเธฒเนเธเนเธเนเธฒเธข</h1>{loading?<div className="panel" role="status">เธเธณเธฅเธฑเธเนเธซเธฅเธ”เธเธดเธฅ...</div>:bill?<><p>{bill.venue_name} โ€ข {bill.booking_date} โ€ข {bill.start_time}โ€“{bill.end_time}</p><div className="split-preview"><b>{bill.name}</b><br/>เธขเธญเธ”เธ—เธตเนเธ•เนเธญเธเธเนเธฒเธข <b>เธฟ{bill.amount.toLocaleString()}</b></div>{bill.paid||done?<div className="panel" role="status"><b>เธเธณเธฃเธฐเนเธฅเนเธง โ“</b><p>เธฃเธฒเธขเธเธฒเธฃเธเธตเนเนเธ”เนเธฃเธฑเธเธเธฒเธฃเธขเธทเธเธขเธฑเธเนเธฅเนเธง</p></div>:<><div className="auth-error">{error}</div><button type="button" className="auth-submit" disabled={busy} onClick={pay}>{busy?'เธเธณเธฅเธฑเธเธขเธทเธเธขเธฑเธ...':'เธเธฑเธเธเนเธฒเธขเนเธฅเนเธง'}</button></>}</>:<div className="panel" role="alert"><b>เนเธกเนเธชเธฒเธกเธฒเธฃเธ–เน€เธเธดเธ”เธฅเธดเธเธเนเธเธตเนเนเธ”เน</b><p>{error||'เธฅเธดเธเธเนเธญเธฒเธเธซเธกเธ”เธญเธฒเธขเธธเธซเธฃเธทเธญเนเธกเนเธ–เธนเธเธ•เนเธญเธ'}</p><button type="button" className="auth-submit" onClick={()=>void load()}>เธฅเธญเธเนเธซเธกเน</button></div>}</div></main></div>
}
function Auth({close,onUser}:{close:()=>void;onUser:(u:User)=>void}){
 useEffect(()=>{const onKey=(e:globalThis.KeyboardEvent)=>{if(e.key==='Escape')close()};document.addEventListener('keydown',onKey);return()=>document.removeEventListener('keydown',onKey)},[close])
 const [mode,setMode]=useState<'register'|'login'|'forgot'|'reset'>('register'),[name,setName]=useState(''),[email,setEmail]=useState(''),[password,setPassword]=useState(''),[phone,setPhone]=useState(''),[address,setAddress]=useState(''),[code,setCode]=useState(''),[error,setError]=useState(''),[busy,setBusy]=useState(false),[devCode,setDevCode]=useState('')
 const submit=async()=>{setError('');if(!email||!password||(mode==='register'&&(!name||!phone||!address))){setError('กรุณากรอกข้อมูลให้ครบ');return}setBusy(true);try{const r=mode==='register'?await api.register(name,email,password,phone,address):await api.login(email,password);localStorage.setItem('porsball_token',r.token);onUser(r.user);close()}catch(e){setError(e instanceof Error?e.message:'ไม่สามารถเชื่อมต่อเซิร์ฟเวอร์ได้')}finally{setBusy(false)}}
 const sendCode=async()=>{setError('');setDevCode('');if(!email){setError('เธเธฃเธธเธ“เธฒเธเธฃเธญเธเธญเธตเน€เธกเธฅ');return}setBusy(true);try{const r=await api.forgotPassword(email);setDevCode(r.devCode||'');setMode('reset')}catch(e){setError(e instanceof Error?e.message:'เนเธกเนเธชเธฒเธกเธฒเธฃเธ–เธชเนเธเธฃเธซเธฑเธชเนเธ”เน')}finally{setBusy(false)}}
 const reset=async()=>{setError('');if(!email||!/^[0-9]{6}$/.test(code)||password.length<8){setError('เธเธฃเธธเธ“เธฒเธเธฃเธญเธ OTP 6 เธซเธฅเธฑเธ เนเธฅเธฐเธฃเธซเธฑเธชเธเนเธฒเธเนเธซเธกเนเธญเธขเนเธฒเธเธเนเธญเธข 8 เธ•เธฑเธงเธญเธฑเธเธฉเธฃ');return}setBusy(true);try{await api.resetPassword(email,code,password);setError('เน€เธเธฅเธตเนเธขเธเธฃเธซเธฑเธชเธเนเธฒเธเน€เธฃเธตเธขเธเธฃเนเธญเธขเนเธฅเนเธง โ“');setMode('login');setPassword('');setCode('');setDevCode('')}catch(e){setError(e instanceof Error?e.message:'เน€เธเธฅเธตเนเธขเธเธฃเธซเธฑเธชเธเนเธฒเธเนเธกเนเธชเธณเน€เธฃเนเธ')}finally{setBusy(false)}}
 if(mode==='forgot'||mode==='reset') return <div className="auth-overlay"><div className="auth-box"><button type="button" className="auth-x" onClick={close} aria-label="เธเธดเธ”เธซเธเนเธฒเธ•เนเธฒเธ" title="เธเธดเธ”">ร—</button><div className="auth-logo"><b>Pors</b><span>Ball</span> โฝ</div>{mode==='forgot'?<><h2>เธฅเธทเธกเธฃเธซเธฑเธชเธเนเธฒเธ</h2><p>เธเธฃเธญเธเธญเธตเน€เธกเธฅเธ—เธตเนเนเธเนเธชเธกเธฑเธเธฃ เน€เธฃเธฒเธเธฐเธชเนเธเธฃเธซเธฑเธชเธขเธทเธเธขเธฑเธเนเธซเน</p><input value={email} onChange={e=>setEmail(e.target.value)} type="email" placeholder="เธญเธตเน€เธกเธฅ"/><div className="auth-error">{error}</div><button type="button" className="auth-submit" disabled={busy} onClick={sendCode}>{busy?'เธเธณเธฅเธฑเธเธชเนเธ...':'เธชเนเธเธฃเธซเธฑเธชเธขเธทเธเธขเธฑเธ'}</button><button type="button" className="auth-toggle" onClick={()=>{setMode('login');setError('')}}>เธเธฅเธฑเธเน€เธเนเธฒเธชเธนเนเธฃเธฐเธเธ</button></>:<><h2>เธขเธทเธเธขเธฑเธเนเธฅเธฐเธ•เธฑเนเธเธฃเธซเธฑเธชเธเนเธฒเธเนเธซเธกเน</h2><p>เธชเนเธเธฃเธซเธฑเธชเนเธเธ—เธตเน {email}</p>{devCode&&<p className="auth-error">เนเธซเธกเธ”เธ—เธ”เธชเธญเธ: OTP เธเธทเธญ {devCode}</p>}<input value={code} onChange={e=>setCode(e.target.value.replace(/\D/g,'').slice(0,6))} inputMode="numeric" placeholder="เธฃเธซเธฑเธชเธขเธทเธเธขเธฑเธ 6 เธซเธฅเธฑเธ"/><input value={password} onChange={e=>setPassword(e.target.value)} type="password" placeholder="เธฃเธซเธฑเธชเธเนเธฒเธเนเธซเธกเน (เธญเธขเนเธฒเธเธเนเธญเธข 8 เธ•เธฑเธงเธญเธฑเธเธฉเธฃ)"/><div className="auth-error">{error}</div><button type="button" className="auth-submit" disabled={busy} onClick={reset}>{busy?'เธเธณเธฅเธฑเธเน€เธเธฅเธตเนเธขเธ...':'เน€เธเธฅเธตเนเธขเธเธฃเธซเธฑเธชเธเนเธฒเธ'}</button><button type="button" className="auth-toggle" onClick={()=>{setMode('forgot');setError('')}}>เธชเนเธเธฃเธซเธฑเธชเนเธซเธกเน</button></>}</div></div>
 return <div className="auth-overlay"><div className="auth-box"><button type="button" className="auth-x" onClick={close} aria-label="ปิดหน้าต่าง" title="ปิด">×</button><div className="auth-logo"><b>Pors</b><span>Ball</span> ⚽</div><h2>{mode==='register'?'สมัครสมาชิก':'เข้าสู่ระบบ'}</h2><p>{mode==='register'?'สร้างบัญชีเพื่อจองสนามและหาเพื่อนร่วมทีม':'ยินดีต้อนรับกลับมา'}</p>{mode==='register'&&<><input value={name} onChange={e=>setName(e.target.value)} placeholder="ชื่อของคุณ"/><input value={phone} onChange={e=>setPhone(e.target.value)} inputMode="tel" placeholder="เบอร์โทร"/><input value={address} onChange={e=>setAddress(e.target.value)} placeholder="ที่อยู่"/></>}<input value={email} onChange={e=>setEmail(e.target.value)} type="email" placeholder="อีเมล"/><input value={password} onChange={e=>setPassword(e.target.value)} type="password" placeholder="รหัสผ่าน"/><div className="auth-error">{error}</div><button type="button" className="auth-submit" disabled={busy} onClick={submit}>{busy?'กำลังดำเนินการ...':mode==='register'?'สมัครสมาชิก':'เข้าสู่ระบบ'}</button>{mode==='login'&&<button type="button" className="auth-toggle" onClick={()=>{setMode('forgot');setError('')}}>ลืมรหัสผ่าน?</button>}<button type="button" className="auth-toggle" onClick={()=>{setMode(mode==='register'?'login':'register');setError('')}}>{mode==='register'?'มีบัญชีแล้ว? เข้าสู่ระบบ':'ยังไม่มีบัญชี? สมัครสมาชิก'}</button></div></div>
}
